VPC peering -> VPC A <-> VPC B

Bidirectional peering  

create keypair -> aws ec2 keypair

VPC primary secondary
Subnet public and private
Create EC2 in both the instances, user data script for nginx
Create Data source to take AMI
IGW
Route table

VPC peering

A-> B

Source VPC A ID, Region

dest Peer VPC B ID, peer Region

B -> A

Source VPC B ID, Region

dest Peer VPC A ID, peer Region

Route creation on route table

auto accept true, false